% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.ivc}
\alias{plot.ivc}
\alias{ivc_plot}
\title{Plot the IVC diagnostic}
\usage{
\method{plot}{ivc}(x, ...)

ivc_plot(x, ...)
}
\arguments{
\item{x}{An object of class \code{"ivc"} from \code{\link[=compare_iv_cv]{compare_iv_cv()}}.}

\item{...}{Further arguments passed to the underlying plotting calls.}
}
\value{
\code{x}, invisibly. Called for the side effect of drawing a plot.
}
\description{
Draws the two treatment-effect estimates on a common scale and displays the
diagnostic contrast \eqn{\Delta} with its confidence interval. A base-graphics
plot is used so the package has no plotting dependencies.
}
\examples{
d <- ivc_simulate(n = 1000, gamma = 1, kappa = 0.3, seed = 1)
fit <- compare_iv_cv(d, "Y", "P", "A", "Z", se = "mom")
plot(fit)
}
