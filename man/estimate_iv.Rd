% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_iv}
\alias{estimate_iv}
\title{Conventional instrumental-variable (Wald) estimate}
\usage{
estimate_iv(
  data,
  outcome,
  pretest,
  treat,
  instrument,
  covariates = NULL,
  weights = NULL,
  se = FALSE,
  cluster = NULL
)
}
\arguments{
\item{data}{A data frame containing the analysis variables.}

\item{outcome}{Name of the posttest outcome column (\eqn{Y}).}

\item{pretest}{Name of the pretest column (\eqn{P}). Accepted for a common
interface with \code{\link[=estimate_cv]{estimate_cv()}}; not used by the IV estimate itself.}

\item{treat}{Name of the treatment column (\eqn{A}).}

\item{instrument}{Name of the single instrument column (\eqn{Z}).}

\item{covariates}{Optional character vector of covariate column names to
partial out. Default \code{NULL} reproduces the covariate-free Wald estimate.}

\item{weights}{Optional survey weights: either a numeric vector of length
\code{nrow(data)} or the name of a column in \code{data}.}

\item{se}{If \code{TRUE}, returns a named vector with the estimate and its
analytic standard error (\code{estimate}, \code{se}). The standard error is the
heteroskedasticity-robust just-identified IV standard error, taken from
the diagonal of the same GMM sandwich used by \code{\link[=compare_iv_cv]{compare_iv_cv()}}. Default
\code{FALSE} returns the point estimate only.}

\item{cluster}{Optional cluster identifier (column name in \code{data} or a
vector of length \code{nrow(data)}), e.g. school. When supplied and
\code{se = TRUE}, a cluster-robust standard error is returned. The point
estimate is unaffected. With few clusters (roughly G < 40) the
cluster-robust SE can be optimistic (Cameron & Miller, 2015).}
}
\value{
A numeric scalar (the IV estimate), or a named vector
\code{c(estimate, se)} when \code{se = TRUE}.
}
\description{
Computes the just-identified instrumental-variable estimate of a treatment
effect in a pretest-posttest design. With no covariates this is the Wald
ratio \eqn{\widehat{\tau}_{IV} = \mathrm{cov}(Z, Y) / \mathrm{cov}(Z, A)};
when covariates are supplied they are partialled out by weighted least
squares (Frisch-Waugh-Lovell) before the ratio is formed.
}
\examples{
d <- ivc_simulate(n = 1000, gamma = 1, kappa = 0, seed = 1)
estimate_iv(d, outcome = "Y", pretest = "P", treat = "A", instrument = "Z")
estimate_iv(d, "Y", "P", "A", "Z", se = TRUE)
}
\seealso{
\code{\link[=estimate_cv]{estimate_cv()}}, \code{\link[=compare_iv_cv]{compare_iv_cv()}}
}
