% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{ivc_simulate}
\alias{ivc_simulate}
\title{Simulate a pretest-posttest dataset with a shared confounder}
\usage{
ivc_simulate(
  n,
  gamma,
  kappa,
  tau = 0.5,
  beta_A = 1,
  beta_P = 1,
  beta_Y = 1,
  sd_eU = 1,
  sd_eP = 1,
  sd_eY = 1,
  sd_eA = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{gamma}{Instrument strength (path \eqn{Z \to A}).}

\item{kappa}{Exogeneity-violation strength (path \eqn{Z \to U});
\code{kappa = 0} yields a valid instrument.}

\item{tau}{True treatment effect. Default \code{0.5}.}

\item{beta_A, beta_P, beta_Y}{Loadings of \eqn{U} on \eqn{A}, \eqn{P}, \eqn{Y}.}

\item{sd_eU, sd_eP, sd_eY, sd_eA}{Error standard deviations.}

\item{seed}{Optional integer seed.}
}
\value{
A data frame with columns \code{Z}, \code{U}, \code{A}, \code{P}, \code{Y} (the latent \code{U}
is returned for reference and is not used by the estimators).
}
\description{
Generates data under the single-factor confounding model used in the IVC
simulation study. A latent confounder \eqn{U} loads on the pretest \eqn{P},
the posttest \eqn{Y}, and the treatment \eqn{A}; the instrument \eqn{Z}
shifts the treatment with strength \code{gamma}, and violates exogeneity through
a path into \eqn{U} of strength \code{kappa} (\code{kappa = 0} is a valid instrument).
}
\details{
The structural model is
\deqn{U = \kappa Z + e_U,\quad A = \gamma Z + \beta_A U + e_A,}
\deqn{P = \beta_P U + e_P,\quad Y = \tau A + \beta_Y U + e_Y,}
with all error terms standard normal (scaled by the corresponding \verb{sd_*}).
}
\examples{
d <- ivc_simulate(n = 500, gamma = 1, kappa = 0.2, seed = 42)
head(d)
}
