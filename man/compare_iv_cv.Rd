% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{compare_iv_cv}
\alias{compare_iv_cv}
\title{Compare IV and compass estimates: the IV-Compass (IVC) exogeneity diagnostic}
\usage{
compare_iv_cv(
  data,
  outcome,
  pretest,
  treat,
  instrument,
  covariates = NULL,
  weights = NULL,
  se = c("bootstrap", "mom"),
  level = 0.95,
  n_boot = 2000,
  seed = NULL,
  cluster = NULL,
  weak_loading_warn = 2
)
}
\arguments{
\item{data}{A data frame containing the analysis variables.}

\item{outcome}{Name of the posttest outcome column (\eqn{Y}).}

\item{pretest}{Name of the pretest column (\eqn{P}). Accepted for a common
interface with \code{\link[=estimate_cv]{estimate_cv()}}; not used by the IV estimate itself.}

\item{treat}{Name of the treatment column (\eqn{A}).}

\item{instrument}{Name of the single instrument column (\eqn{Z}).}

\item{covariates}{Optional character vector of covariate column names to
partial out. Default \code{NULL} reproduces the covariate-free Wald estimate.}

\item{weights}{Optional survey weights: either a numeric vector of length
\code{nrow(data)} or the name of a column in \code{data}.}

\item{se}{Method for quantifying uncertainty in \eqn{\Delta}: \code{"bootstrap"}
(adaptive percentile bootstrap, the default) or \code{"mom"} (analytic
method-of-moments standard error with a normal interval).}

\item{level}{Confidence level for the interval. Default \code{0.95}.}

\item{n_boot}{Target number of valid bootstrap replicates (used when
\code{se = "bootstrap"}). Default \code{2000}.}

\item{seed}{Optional integer seed for reproducible bootstrap resampling.}

\item{cluster}{Optional cluster identifier (column name or vector), e.g.
school. With \code{se = "mom"} a cluster-robust sandwich is used; with
\code{se = "bootstrap"} whole clusters are resampled (Lee & Kim, 2026,
JEEV 39(2)). Point estimates are unaffected. With few clusters (roughly
G < 40) the cluster-robust SE can be optimistic (Cameron & Miller, 2015);
no finite-sample degrees-of-freedom correction is applied.}

\item{weak_loading_warn}{Threshold on the |z| statistic of the compass
loading denominator \eqn{\mathrm{cov}(P, Z \mid A)} below which a warning
about an unstable ratio \eqn{\widehat{\delta}} is issued. Default \code{2}.
Set to \code{0} to disable. See \code{\link[=estimate_cv]{estimate_cv()}}.}
}
\value{
An object of class \code{"ivc"}: a list with elements \code{tau_IV},
\code{tau_comp}, \code{delta_hat}, \code{Delta}, \code{se}, \code{ci} (length-2 vector),
\code{p_value}, \code{reject} (logical, whether the interval excludes zero),
\code{method}, \code{level}, \code{n}, and \code{call}. Has \code{print}, \code{summary}, and \code{plot}
methods.
}
\description{
Implements the IVC diagnostic by contrasting the conventional IV estimate
\eqn{\widehat{\tau}_{IV}} with the compass-corrected estimate
\eqn{\widehat{\tau}_{comp}}. Under a valid instrument the two estimands
coincide, so the contrast \eqn{\Delta = \widehat{\tau}_{IV} -
\widehat{\tau}_{comp}} has expectation zero. A confidence interval for
\eqn{\Delta} that excludes zero is read as evidence against single-instrument
exogeneity. Uncertainty is quantified either by an adaptive percentile
bootstrap or by an analytic method-of-moments (GMM-style) standard error.
}
\examples{
d <- ivc_simulate(n = 1000, gamma = 1, kappa = 0.3, seed = 1)
fit <- compare_iv_cv(d, outcome = "Y", pretest = "P", treat = "A",
                     instrument = "Z", se = "mom")
fit
}
\seealso{
\code{\link[=estimate_iv]{estimate_iv()}}, \code{\link[=estimate_cv]{estimate_cv()}}, \code{\link[=ivc_power]{ivc_power()}}
}
