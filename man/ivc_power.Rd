% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{ivc_power}
\alias{ivc_power}
\title{Simulation-based power analysis for the IVC diagnostic}
\usage{
ivc_power(
  n,
  gamma,
  kappa,
  n_rep = 500,
  se = c("mom", "bootstrap"),
  level = 0.95,
  n_boot = 1000,
  tau = 0.5,
  beta_A = 1,
  beta_P = 1,
  beta_Y = 1,
  sd_eU = 1,
  sd_eP = 1,
  sd_eY = 1,
  sd_eA = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size per replicate.}

\item{gamma}{Instrument strength.}

\item{kappa}{Exogeneity-violation strength (\code{0} = valid instrument).}

\item{n_rep}{Number of Monte Carlo replicates. Default \code{500}.}

\item{se}{Uncertainty method passed to \code{\link[=compare_iv_cv]{compare_iv_cv()}}: \code{"mom"} (default,
fast) or \code{"bootstrap"}.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{n_boot}{Bootstrap replicates when \code{se = "bootstrap"}.}

\item{tau, beta_A, beta_P, beta_Y, sd_eU, sd_eP, sd_eY, sd_eA}{Passed to
\code{\link[=ivc_simulate]{ivc_simulate()}}.}

\item{seed}{Optional integer seed.}
}
\value{
A list of class \code{"ivc_power"} with the detection \code{rate}, the number
of valid replicates \code{n_valid}, and the design settings.
}
\description{
Estimates the detection (rejection) rate of the IVC diagnostic by repeatedly
simulating data from \code{\link[=ivc_simulate]{ivc_simulate()}} under a given design and recording how
often the diagnostic interval for \eqn{\Delta} excludes zero. When
\code{kappa = 0} the returned rate is an empirical Type I error rate; when
\code{kappa > 0} it is empirical power.
}
\examples{
\donttest{
ivc_power(n = 1000, gamma = 1, kappa = 0.2, n_rep = 100, se = "mom")
}
}
