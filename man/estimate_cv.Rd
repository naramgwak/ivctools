% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_cv}
\alias{estimate_cv}
\title{Compass-corrected treatment-effect estimate}
\usage{
estimate_cv(
  data,
  outcome,
  pretest,
  treat,
  instrument,
  covariates = NULL,
  weights = NULL,
  return_delta = FALSE,
  se = FALSE
)
}
\arguments{
\item{data}{A data frame containing the analysis variables.}

\item{outcome}{Name of the posttest outcome column (\eqn{Y}).}

\item{pretest}{Name of the pretest column (\eqn{P}). Accepted for a common
interface with \code{\link[=estimate_cv]{estimate_cv()}}; not used by the IV estimate itself.}

\item{treat}{Name of the treatment column (\eqn{A}).}

\item{instrument}{Name of the single instrument column (\eqn{Z}).}

\item{covariates}{Optional character vector of covariate column names to
partial out. Default \code{NULL} reproduces the covariate-free Wald estimate.}

\item{weights}{Optional survey weights: either a numeric vector of length
\code{nrow(data)} or the name of a column in \code{data}.}

\item{return_delta}{If \code{TRUE}, includes the loading ratio \code{delta_hat} in the
output. Default \code{FALSE}.}

\item{se}{If \code{TRUE}, includes analytic standard error(s) in the output: the
SE of \code{tau_comp} (and of \code{delta_hat} when \code{return_delta = TRUE}), taken
from the diagonal of the GMM sandwich used by \code{\link[=compare_iv_cv]{compare_iv_cv()}}.}
}
\value{
A numeric scalar \eqn{\widehat{\tau}_{comp}}, or a named numeric
vector when \code{return_delta} and/or \code{se} are \code{TRUE} (elements among
\code{tau_comp}, \code{delta_hat}, \code{se_tau_comp}, \code{se_delta_hat}).
}
\description{
Computes the compass (detection-variable corrected) estimate
\eqn{\widehat{\tau}_{comp}} for a pretest-posttest design. The instrument
\eqn{Z} is used as a detection variable: residualizing \eqn{Z}, \eqn{Y}, and
\eqn{P} on the treatment \eqn{A} yields a loading ratio
\eqn{\widehat{\delta}}, the posttest is corrected to
\eqn{Y^* = Y - \widehat{\delta} P}, and \eqn{\widehat{\tau}_{comp}} is the
coefficient of \eqn{A} in a regression of \eqn{Y^*} on \eqn{A}. Unlike
\code{\link[=estimate_iv]{estimate_iv()}}, this estimate targets the causal effect even when \eqn{Z}
violates exogeneity, provided it is a valid detection variable.
}
\examples{
d <- ivc_simulate(n = 1000, gamma = 1, kappa = 0.2, seed = 1)
estimate_cv(d, outcome = "Y", pretest = "P", treat = "A",
            instrument = "Z", return_delta = TRUE, se = TRUE)
}
\seealso{
\code{\link[=estimate_iv]{estimate_iv()}}, \code{\link[=compare_iv_cv]{compare_iv_cv()}}
}
